# ── Frontend env vars ─────────────────────────────────────────────────────────
# Point to your Railway backend URL in production
VITE_API_BASE_URL=https://your-backend.railway.app

# Set to 'true' only during local development to use mock responses
VITE_USE_MOCK=false
